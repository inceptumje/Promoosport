{% extends 'Dashboard.html.twig' %}

{% block title %}Ajouter Promosport{% endblock %}

{% block body %}
    <div class="page-header">
        <h3 class="page-title">Ajouter Promosport </h3>
    </div>
    {{ form_start(form) }}
       {{ form_start(form) }}
            {{ form_row(form.type)}}
            {{ form_row(form.content) }}

            {% if modif %}
                <button class="btn btn-success">Ajouter</button>
            {% else %}
                <button class="btn btn-success">Modifier</button>
            {% endif %}
    {{ form_end(form) }}

{% endblock %}
