{% extends 'Dashboard.html.twig' %}

{% block title %}Ajouter Pointe vente{% endblock %}

{% block body %}
    <div class="page-header">
        <h3 class="page-title">Ajouter Pointe vente </h3>
    </div>
    {{ form_start(form) }}
       {{ form_start(form) }}
            {{ form_row(form.state)}}
            {{ form_row(form.client) }}
            {{ form_row(form.code) }}
            {{ form_row(form.lat) }}
            {{ form_row(form.lon) }}
            {% if modif %}
                <button class="btn btn-success">Ajouter</button>
            {% else %}
                <button class="btn btn-success">Modifier</button>
            {% endif %}
    {{ form_end(form) }}

{% endblock %}
